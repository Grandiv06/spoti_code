# API base URL (origin only — service paths include /api/...)
NEXT_PUBLIC_API_BASE_URL=https://spoticode.vercel.app

# Set to "true" to use local mock responses instead of the live API
NEXT_PUBLIC_USE_MOCK_API=false
